This project uses the PDcurses library contained,

Can also be ran on a working Windows computer with cygwin by navigating to the proper directory and
compiling with g++ ./2048.cpp ./pdcurses.lib -o 2048.exe
and then runing the 2048.exe

Contains simple savegame and highscore features.
